import React from "react";
import MainScene from "./scenes/MainScene";

const App: React.FC = () => {
  return (
    <div style={{ height: "100vh" }}>
      <MainScene />
    </div>
  );
};

export default App;
